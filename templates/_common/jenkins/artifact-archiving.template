aspects:
  - name: jenkins/artifact-archiving.archive-artifacts
    aspect: archive-artifacts
    conditions:
      archive.patterns: .*
    variables:
      aspect.publisher-constraints.archive-artifacts:
        - [ after, <all> ]
      aspect.archive-artifacts.file-pattern: ${archive.patterns:${mode}|${archive.patterns}}
